'use client';
import Link from 'next/link';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';import {API,saveAuth} from '../../lib/api';
export default function Register(){const router=useRouter();const[name,setName]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);
async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await fetch(`${API}/v1/auth/register`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,password})});const b=await r.json();if(!r.ok)throw new Error(b.detail||'Registration failed');saveAuth(b);router.push('/')}catch(e){setError(String(e))}finally{setBusy(false)}}
return <div className="authWrap"><div className="authCard"><Link href="/" className="brand authBrand"><span className="brandMark">B</span><span>BharatVideo <b>AI</b></span></Link><span className="eyebrow">START FREE</span><h1>Create your studio</h1><p>Get 30 credits and claim your existing local projects automatically if this is the first account.</p><form onSubmit={submit}><div className="field"><label>Name</label><input required minLength={2} value={name} onChange={e=>setName(e.target.value)} /></div><div className="field"><label>Email</label><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} /></div><div className="field"><label>Password</label><input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} /></div>{error&&<div className="errorBox">{error}</div>}<button className="primaryButton jumbo" disabled={busy}>{busy?'Creating…':'Create free account'}</button></form><small>Already have an account? <Link href="/login">Login</Link></small></div></div>}
