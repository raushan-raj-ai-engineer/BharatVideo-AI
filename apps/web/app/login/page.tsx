'use client';
import Link from 'next/link';import {FormEvent,useState} from 'react';import {useRouter,useSearchParams} from 'next/navigation';import {API,saveAuth} from '../../lib/api';
export default function Login(){const router=useRouter();const q=useSearchParams();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);
async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await fetch(`${API}/v1/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const b=await r.json();if(!r.ok)throw new Error(b.detail||'Login failed');saveAuth(b);router.push(q.get('next')||'/')}catch(e){setError(String(e))}finally{setBusy(false)}}
return <div className="authWrap"><div className="authCard"><Link href="/" className="brand authBrand"><span className="brandMark">B</span><span>BharatVideo <b>AI</b></span></Link><span className="eyebrow">WELCOME BACK</span><h1>Login to Creator Studio</h1><p>Continue your projects, credits and renders.</p><form onSubmit={submit}><div className="field"><label>Email</label><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} /></div><div className="field"><label>Password</label><input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} /></div>{error&&<div className="errorBox">{error}</div>}<button className="primaryButton jumbo" disabled={busy}>{busy?'Signing in…':'Login'}</button></form><small>New here? <Link href="/register">Create free account</Link></small></div></div>}
